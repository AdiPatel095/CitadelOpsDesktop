# Desktop localization coverage ledger

Status: partial implementation, not a translated release. Baseline: e77ed022c2f926ae3509527fdabf54dca9e22586. There are 1376 typed source keys: 1322 custom messages and54 verified official game-key routes. German, French, Spanish, Brazilian Portuguese and Arabic each contain155 model-authored custom entries; the other20 non-English catalogs contain92 each. Missing entries are1167 in those five packs and1230 in the other20. Language-neutral numeric/ID templates retain their semantics; English fallbacks are not credited as translated. Human linguistic review remains pending.

The source-aware static migration assigns 729 standalone JSX sinks across 64 files to 712 file-context keys. Of these, 21 keys / 28 sinks use reviewed official game terms. A second source-aware pass assigns 505 static visible attributes / 488 keys across 61 files, with six more official keys (eight sinks) for reviewed kingdom, commander, castellan and combat-tool labels. `attribute-migrations.json` records those source assignments and its 37 explicit exclusions. `static-migrations.json` records the source location, route, and explicit rich/dynamic/technical exclusions. `ui.en.json` is generated by the strict gate for translation tooling; source-of-truth keys remain in `messages.ts`, `sourceMessages.ts` and `richMessages.ts`.

## Source inventory

Run `node scripts/inventory-localization.mjs` from Client. `source-inventory.json` contains conservative AST candidates with source references and sink categories. It includes branch values and technical strings in rendered expressions, so counts are not translation counts. Exclusions retain location/reason without duplicating technical text. Missing source candidates, dynamic string composition and false exclusions require semantic review before coverage certification. No fallback English counts as translated.

## Surface routes and remaining work

| Surface | Source ownership | Required message route | Current gap |
| --- | --- | --- | --- |
| Shell/navigation/header/mobile controls | App, Header, Sidebar, ui components | Explicit typed custom keys for JSX, aria and titles | Navigation/sidebar/theme converted; standalone static text assigned throughout 64 files; header dynamic status and remaining attributes need conversion |
| Settings/forms/confirmation/modals | settings/components, SettingsView | Explicit keys; interpolate values once; preserve user input | Standalone text assigned; attributes, dynamic validation, rich help and templates remain |
| Automation/event views | views, events, attackAnalytics | Custom descriptors; official `localizationKey` first for event names | EventsView currently overrides event IDs with English names |
| Units/buildings/decorations/equipment/gems/effects/currencies | MetadataContext, equipment components | Official keys via per-request viewer locale; custom generic fallback descriptors | Metadata locale invalidation implemented; display fallback provenance needs consumer presentation |
| Dynamic server errors/operation notifications | CitadelClient, ApiContext, OperationNotifications | Adjacent `messageDescriptor` / `detailDescriptor`; legacy English fallback marked uncovered | APIError preserves validated descriptors and settings retains reactive errors; other consumers remain English |
| Activity dock/channel names | LoggerDock | Structured server message descriptors, preserve timestamps/identities | Aligned structured descriptors render reactively with batched official keys; raw history explicitly marked untranslated; surrounding status/empty prose and catalogs remain |
| Event/world history and rankings | WorldEventHistory, FeatureEventHistory, ranking components | Official event keys + typed summary descriptors; Intl values | Server-built names and historic English strings remain |
| Exports/copy | LoggerDock, SettingsView, SettingsTransfer | Localized display copy; settings JSON keys and values remain machine/user data | Activity copy uses the rendered message text; untranslated event/direction labels remain; settings filenames are product identifiers |
| Empty/loading/error/status states | All views/components | Explicit custom keys and ICU plural/select | Mostly uncovered |
| Patch notes/support | config/PatchNotes, PatchNotesView, SupportView | Authored custom catalog content | All historic prose remains English |
| Date/number/duration/count formatters | views, components, equipment, analytics | Intl formatter using viewer locale and ICU | Provider formatter available; legacy formatters remain |
| User-created castle/player/alliance/preset/account names | Dynamic payloads and editable state | Verbatim isolated text; never translate | Preserve during conversion |
| Protocol IDs, settings keys, operation IDs, diagnostic console | Contracts and technical code | Excluded from translation | Console exclusion applies only to private diagnostics, not displayed LoggerDock content |

## Completion gate

`npm run check:localization` intentionally fails while source candidates or dynamic surfaces remain open. Build/test success alone is not translation completion. Catalogs are model-authored and need human linguistic review; official translations retain official provenance even when their text equals English.


Every visible sink needs a reviewed key or a specific exclusion. The strict release gate requires every supported custom catalog to have exactly the English keys and matching recursively parsed ICU argument sets. Unit tests separately validate authored subsets without treating missing entries as complete; missing counts are explicit in the gate. Official game positional placeholders are separate from ICU. Runtime unknown descriptors, unavailable official keys and retained English history must remain visible as coverage gaps. Check all 26 locales, long text, Arabic direction, locale changes during in-flight fetches, offline fallback, account switching, exports and accessibility. Existing physical left/right CSS and all non-header responsive surfaces still need RTL audit.

## Latest implementation checkpoint

Nine whole rich paragraphs use safe ICU tag callbacks; technical code fragments are scalar arguments, and numeric costs use locale formatting. Sixty repeated generated keys were consolidated into twenty reviewed semantic targets; the explicit map is `common-key-map.json`. These are source assignments, not completed translations. The inventory retains 7374 conservative candidates requiring semantic review.

Equipment calculations, deduplication and inferred priorities use canonical English effect metadata independently of selected display text. Same-version locale transitions retain canonical state; failed canonical loads expose an unavailable status and pause reconfiguration. Two verified Japanese v4357 sign defects have explicit versioned display corrections, preserving raw official source. Tests cover real English, Arabic and Japanese strings, German display alternatives, loading/failure stability, unknown placeholders and numeric/identity separation.

Server and backend catalogs are independently owned and lazily loaded. Their partial authored entries are validated and missing entries remain part of the strict gate; synchronization never credits English fallbacks as translated. Browser verification on a disconnected local preview confirmed the explicit unavailable equipment status, disabled actions and Arabic viewer selection; it does not establish connected runtime or full RTL acceptance.

Canonical readiness correction: public items 786.03 / language 4357 were audited against all 830 effect definitions. The explicit identity manifest records 671 usable templates, 156 missing descriptions and three previously retired descriptions. Healthy real data reaches ready; unexpected empty/partial canonical dictionaries retain prior same-scope state. Documented absences preserve existing raw-value/type behavior and display a provenance notice when selected. An unrelated version bump is permitted when exact identities remain compatible; newly available templates supersede documented absences. Source URLs and hashes are in `canonicalEffectCoverage.v4357.json`. Build and 157 tests pass; strict coverage remains incomplete.

Thirty equipment battle-target labels/descriptions and five slot labels now have reactive explicit keys; ten labels route to verified official concepts. Canonical optimizer target identities remain unchanged. Other pure optimizer target summaries still require migration.

Eight common controls are now directly model-authored in all 25 non-English packs (200 additional translations), with source hashes in `common-authorship.json`; linguistic review remains pending. Event equipment names/descriptions, tier selectors, count badges, availability text and the whole leader-selection sentence now use explicit reactive messages. Protocol event/tier values remain unchanged. Three event labels use verified official event-title keys. Completion notifications in EquipmentView and other modal families remain open.

Server catalog sync34d62821 contains 2,868 source keys and709 authored entries across25 partial packs. Backend has12 complete282-key packs. The strict gate reports remaining source-minus-pack coverage for both owners.

`official-effect-argument-audit.json` documents the only two audited game templates with positional index2. Their raid-boss-stage effects have no current Client display consumer; this is an explicit reachability exclusion, not a claim that unknown arguments are rendered correctly. Future generic arrivals preserve unresolved tokens.

## Modal and notification checkpoint

The equipment modal module now has63 directly authored entries in de/fr/es/pt/ar, including whole rich sentences, enum-select messages and locale plurals. Authored module inputs and source hashes are retained under `authored/` and `module-authorship.json`. Twenty-seven additional labels route to reviewed official keys; eight formerly authored entries were removed from own-pack counts to avoid duplicate coverage credit. Hollow Moon set linkage to official event128 is recorded in `hollow-moon-official-link.json`; its official event noun is composed into the own PvP designation through the shared descriptor contract.

Notifications retain optional descriptors for headline, aligned detail lines and actions. Alerts validates and renders them reactively. Structured operation failures preserve explanation/recovery and game-code identity, including bounded knowledge context; legacy classifiers and unconverted producer text remain gaps. AutoFortress supply detail map entries consume the additive detailsDescriptors contract with original fallback language metadata.

The external-source gate now rejects missing/empty English families and missing/stale synchronization provenance; negative tests cover server and backend. Select-enum option sets are validated for plain and rich authored catalogs, while locale-specific plural categories are permitted. Build and161 tests pass. Strict completeness still fails.

Reproducible browser evidence is in `tests/localization-browser/README.md`, at http://127.0.0.1:41882/ when its server runs. Synthetic equipment callbacks capture only local JSON; fixture telemetry is fixed data and every other runtime API route is blocked. Open modal, persistent notification and activity locale transitions were checked; Arabic fraction and segmented-indicator defects were corrected. Full shared-control/table/modal RTL audit, remaining module translations and connected integration remain open.
